# Supporting Context

## Notes

This member demonstrates that references may be split from the contract.

## Runtime Map

```mermaid
flowchart LR
    ROOT["Bundle root"] --> MEMBER["Declared member"]
```
