# Member Loading and Provenance

## Requirements

### Every declared member enters the review source set exactly once

Each member preserves its exact bytes, title, role, repository path, and shared bundle digest.
